<li>
	{{name}}

	<button class="change-status" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if hungry}}Order a Burger!{{else}}Eat the Burger!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE</button>
</li>
